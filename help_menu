help: |
     Parameters available with their default values:
       image_name: ansible
       external_repo: "eddieshklaer"
tasks:
- name: help_menu
 debug:
  msg:  "{{ help.split('\n') }}"
 tags: help
 delegate_to: localhost
 run_once: true
